## TODO

- 建立业务与路径的关联，在业务中可以设置路径分组，通过路径分组来过滤路径；
- √ 增加路径分组功能，选择一系列路径作为路径分组
- 增加输出模板功能，可以将路径图形化输出，配置标题，修饰符，图框，路径分组等
- 故障原因，调整为两级
- √ 修改系统中原有的地图调用接口，使用自己的地图
- √ 修改系统增加高速公路盾标
- √ 地图增加测距功能
- 地图增加本地缓存（留待正式部署统一调试）


目前有几个问题，一个是计算路径的时间太长了（几秒至几十秒不等），另一个是高速模型与我期望的不同，我需要的是一个单线网络模型（就是一条高速公路算一条路径，不分上行下行），基于这两点，研究解决方案，效率需要达到https://github.com/dabreegster/route_snapper的效果（实时显示路径的切换），我的工作范围在省一级范围（或更大，全国范围），每个点之间的距离在几十公里，可以适当抽稀数据）