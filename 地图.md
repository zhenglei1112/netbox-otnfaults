# 服务器地址
192.168.30.34
代理服务器 mihomo
nginx转发

# 如果发现地图不更新/有脏数据：
执行 sudo rm -rf /var/cache/nginx/stadia_cache/* && sudo systemctl restart nginx。

# 如果发现节点慢了：
访问你的控制面板 http://192.168.30.34:9090/ui 切换到美国或自动选择。

# 监控缓存占用：
执行 du -sh /var/cache/nginx/stadia_cache 看看你的 10GB 空间用了多少。